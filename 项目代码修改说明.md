# secure-spatial-db 项目代码修改说明文档

> **项目名称**：前后向隐私的空间密文检索系统（Secure Spatial Database）
> **仓库地址**：`https://github.com/NigulasiLiu/secure-spatial-db.git`
> **本地路径**：`/home/developer/secure-spatial-db`
> **文档日期**：2026年8月1日
> **文档用途**：说明本次针对项目代码所做的全部修改及其原因，供软著资料及相关技术文档同步更新使用。

---

## 一、文档说明

本项目为一套基于 **Spring Boot 3.2.5 (Java 17) + Vue 3 + MySQL 8 + MinIO** 实现的前后向隐私空间密文检索系统。此前已基于该代码编写了软件著作权及配套资料。

今天（2026-08-01）在本地（华为云 CloudIDE 开发桌面）对项目完成了**非 Docker 方式的本地部署**，并在部署过程中发现并修复了 **4 处代码级 Bug**、补充了 **4 份测试数据文档**、完善了 **工程化配置**。这些修改**改变了项目源代码的实际内容**，因此之前基于旧代码编写的软著资料需要同步核对更新。

**特别提醒**：本次所有改动均为「修正已有代码的错误/补齐工程配置」，**未改变系统的核心架构、算法原理、功能模块划分与业务流程**。软著中的功能描述、技术方案、系统架构等核心章节原则上无需改动，仅需在涉及以下具体实现细节的章节做同步核对。

---

## 二、修改总览

| 序号 | 修改类型 | 涉及文件 | 是否影响软著源码 |
|------|----------|----------|------------------|
| 1 | Bug修复（后端JDBC连接） | `backend/src/main/resources/application.yml` | ✔ 是（后端源码配置） |
| 2 | Bug修复（前端无效依赖） | `frontend/package.json` | ✔ 是 |
| 3 | Bug修复（前后端接口契约） | `frontend/src/api/index.js` | ✔ 是（前端源码逻辑） |
| 4 | 新增测试数据文档 | `test-documents/`（4个文件） | ✔ 是（新增文件） |
| 5 | 工程配置完善（.gitignore） | `.gitignore` | 影响源码提交清单 |
| 6 | 目录重命名 | `测试文档/` → `test-documents/` | 影响源码目录清单 |

---

## 三、修改详情与原因

### 3.1 【Bug修复】后端 JDBC 连接 URL 字符集参数错误

- **文件**：`backend/src/main/resources/application.yml`
- **修改前**：
  ```yaml
  url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:ssdb}?useUnicode=true&characterEncoding=utf8mb4&useSSL=false&...
  ```
- **修改后**：
  ```yaml
  url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:ssdb}?useUnicode=true&characterEncoding=UTF-8&useSSL=false&...
  ```
- **修改原因**：
  - 后端启动后，在进行「注册/登录」等操作时返回 **HTTP 500**，日志报错：
    ```
    Caused by: com.mysql.cj.exceptions.WrongArgumentException:
    Unsupported character encoding 'utf8mb4'
    Caused by: java.io.UnsupportedEncodingException: utf8mb4
    ```
  - **根因**：JDBC 连接串中的 `characterEncoding=utf8mb4` 并非合法的 Java 字符集名称。Java 中 MySQL 字符集 `utf8mb4` 对应的标准名称是 **`UTF-8`**。MySQL Connector/J 驱动的 `characterEncoding` 参数收到无法识别的名称时抛出 `UnsupportedEncodingException`，导致数据库无法建立连接，进而引发注册、登录等所有数据库操作失败。
  - **原则**：数据库建表 SQL 中的 `CHARSET=utf8mb4` 是**正确且应保留**的（这是 MySQL 层面的合法字符集），只有 **JDBC URL 参数** 需改为 Java 认得的 `UTF-8`。
- **影响软著资料**：
  - 若软著文档中粘贴了 `application.yml` 的数据库连接配置，需同步改为 `characterEncoding=UTF-8`。
  - 若软著中描述了"系统连接 MySQL 使用 UTF-8 编码"的表述，保持正确无需改动；但若照抄了 `utf8mb4` 字样，请修正。

---

### 3.2 【Bug修复】前端 package.json 中存在无效依赖 vite-plugin-leaflet

- **文件**：`frontend/package.json`
- **修改前**：
  ```json
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.4",
    "vite": "^5.2.8",
    "vite-plugin-leaflet": "^1.0.0"
  }
  ```
- **修改后**：
  ```json
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.4",
    "vite": "^5.2.8"
  }
  ```
- **修改原因**：
  - `vite-plugin-leaflet` 在 **npm 上并不存在**（无效依赖包），且**在项目任何代码中均未被 import/引用**。
  - 执行 `npm install` 时该依赖无法解析安装，导致前端依赖安装失败、无法启动开发服务器。
  - 移除该无效依赖后，前端依赖可正常安装。
- **影响软著资料**：
  - 若软著 **前端技术栈/依赖清单** 章节中列出了 `vite-plugin-leaflet`，请删除该条目。
  - 该依赖从未被使用，因此**不影响软著中功能与技术方案的描述**，仅影响依赖清单的准确性。

---

### 3.3 【Bug修复】前端接口响应校验逻辑与后端契约不一致

- **文件**：`frontend/src/api/index.js`
- **修改前**：
  ```javascript
  api.interceptors.response.use(
    (response) => {
      const data = response.data
      if (data.code === 200) {
        return data
      }
      ElMessage.error(data.message || 'request failed')
      return Promise.reject(data)
    },
    ...
  )
  ```
- **修改后**：
  ```javascript
  api.interceptors.response.use(
    (response) => {
      const data = response.data
      // 后端 ApiResponseDto 返回结构: { success, message, data }
      // success===true 视为业务成功
      if (data && data.success === true) {
        return data
      }
      ElMessage.error(data?.message || 'request failed')
      return Promise.reject(data)
    },
    ...
  )
  ```
- **修改原因**：
  - 注册/登录后端已正确返回 "register success / login success"，但前端**始终弹红叉错误提示、且登录后不跳转**。
  - **根因**：前端响应拦截器以 `data.code === 200` 判断业务成功，但后端 `ApiResponseDto` 的**真实返回结构是 `{ success, message, data }`，并不存在 `code` 字段**。因此 `data.code === 200` 恒为 `false`，所有成功请求都被前端误判为失败。
  - 修正为按后端真实契约 `data.success === true` 判断，并顺带将 `data.message` 改为可选链 `data?.message` 增强健壮性。修复后注册、登录成功可正常跳转。
- **影响软著资料**：
  - 若软著前端章节中描述了"通过 `code === 200` 判断接口成功"的代码片段或逻辑，请同步改为"通过 `success === true`（与后端 ApiResponseDto 契约一致）判断"。
  - 此修改体现了前端的**统一响应处理逻辑**，软著中的"前后端交互 / 接口统一封装"描述可据此保持准确。

---

### 3.4 【新增】测试数据文档（4 个文件）

- **目录**：`test-documents/`（原中文目录名 `测试文档/`，见 3.6 节）
- **新增文件及内容**：

| 文件 | 内容 | 用途 |
|------|------|------|
| `北京地标坐标.txt` | 北京市 12 个主要地标的名称+经纬度（WGS84 制），如天安门、故宫、颐和园、鸟巢等 | 空间检索功能测试的空间点数据 |
| `中关村商圈店铺.txt` | 中关村商圈店铺信息 | 空间检索/商圈查询测试数据 |
| `周边餐厅坐标.csv` | 周边餐厅 CSV 格式坐标数据 | 空间范围查询测试数据 |
| `设备定位记录.json` | 物联网设备（配送车、巡检无人机、共享单车驿站、气象站等）定位 JSON 样本 | IoT 设备定位场景测试数据 |

- **新增原因**：
  - 系统为**空间密文检索系统**，测试需要真实的空间坐标类数据来验证检索、范围查询等功能。
  - 补充多格式（txt/csv/json）、多场景（地标、商圈、餐厅、IoT设备）的测试数据，便于功能演示与回归测试。
- **影响软著资料**：
  - 若软著附录中包含**源代码清单/文件结构**，需在源码清单中**新增以上 4 个文件条目**（或对应的 `test-documents/` 目录）。
  - 若软著中有**测试数据样例**，可引用以上数据作为示例。

---

### 3.5 【工程配置】完善 .gitignore 忽略规则

- **文件**：`.gitignore`
- **修改前**：
  ```
  .idea/*
  ```
  （仅 1 行）
- **修改后**（36 行，按类别整理）：
  ```
  # ==== 本机环境配置文件（不要提交） ====
  backend/.mvn-settings.xml      # Maven 本地镜像配置
  frontend/.npmrc                # npm 本地镜像配置

  # ==== 构建产物 ====
  backend/target/                # 后端 Maven 编译输出
  frontend/dist/                 # 前端构建输出
  *.log
  *.tmp
  提交指南.md                     # 本机备忘

  # ==== 依赖目录 & 锁定文件 ====
  backend/node_modules/
  frontend/node_modules/
  frontend/package-lock.json     # 本机生成，与原始仓库结构不一致

  # ==== 运行期数据 ====
  /data/
  minio-data/
  mysql-data/

  # ==== IDE / 编辑器 ====
  .idea/
  .vscode/
  *.iml

  # ==== 操作系统杂项 ====
  .DS_Store
  Thumbs.db
  ```
- **修改原因**：
  - 本地部署后产生了大量运行期产物（Maven 编译输出 `backend/target/`、前端依赖 `node_modules/`、本地镜像配置等），若不加忽略会导致垃圾文件被误提交、仓库臃肿且泄露本机配置。
  - 完善 `.gitignore` 使仓库保持干净，仅提交源代码与真正必要的文件。
- **影响软著资料**：
  - 软著通常基于**源代码目录/文件清单**登记，`.gitignore` 会过滤掉 `target/`、`node_modules/` 等非源码产物。请确保软著源码清单**不包含**这些被忽略的目录，而应仅包含 `backend/src/`、`frontend/src/` 等实际源码。
  - 注意：`frontend/package-lock.json` 已被忽略（不入库），若软著依赖清单里把它列为源码请一并移除。

---

### 3.6 【工程配置】测试文档目录重命名

- **修改前**：中文目录名 `测试文档/`
- **修改后**：英文目录名 `test-documents/`
- **修改原因**：中文目录名在 git 展示、跨平台协同、URL 引用时可能引起编码/兼容问题，统一改为英文目录名以保证仓库可移植性与可读性。
- **影响软著资料**：若软著源码清单中登记了该目录，请将 `测试文档` 更新为 `test-documents`（或按其完整路径更新）。

---

## 四、与本地部署相关的环境信息（供软著运行环境章节参考）

本次修改均基于**非 Docker 本地直跑**方式完成部署验证，核心技术栈与运行参数如下：

- **后端**：Spring Boot 3.2.5，Java 21（本机运行时版本），服务端口 **8080**
- **前端**：Vite / Vue 3.4，开发服务端口 **5173**，`/api` 代理至 8080
- **数据库**：MySQL 8.0，端口 **3306**，库名 `ssdb`，账户 `ssdb`
  - 数据表：`doc_meta`、`edb_k`、`edb_p`、`ss`、`user`
- **对象存储**：MinIO，API 端口 9000 / 控制台端口 9001
  - 数据桶：`encrypted-files`、`ssdb-bucket`
  - 后端通过环境变量 `MINIO_BUCKET=encrypted-files` 读取所用桶
- **认证**：JWT（`JWT_SECRET` 环境变量配置）

> 注：本机为华为云开发者空间 CloudIDE（ARM/aarch64 架构），无法直接访问 Docker Hub，故采用本地直跑。**该环境与本仓库源码的正确性无关**，仅影响部署方式；软著中的部署方式描述若涉及，请据实核对。

---

## 五、修改后源代码文件清单（软著登记请以此为准）

以下为本次修改后**实际应纳入软著源码**的关键文件（不含被 `.gitignore` 忽略的构建产物）：

```
├── src/                                  # 前端源码（Vue3）
│   └── api/
│       └── index.js                      # 【已修改】前端接口统一封装/响应拦截逻辑
│   └── package.json                      # 【已修改】依赖清单（已移除无效依赖）
├── backend/                              # 后端源码（Spring Boot）
│   └── src/main/resources/
│       └── application.yml               # 【已修改】数据库连接配置
├── test-documents/                       # 【新增】测试数据文档（4个文件）
│   ├── 北京地标坐标.txt
│   ├── 中关村商圈店铺.txt
│   ├── 周边餐厅坐标.csv
│   └── 设备定位记录.json
├── docker-compose.yml
├── README.md
├── .codeup-ci.yml
└── .gitignore                            # 【已修改】忽略规则
```

---

## 六、对软著资料的逐项核对建议（给龙虾的清单）

请对照以下清单核验并更新软著资料：

1. **后端配置章节**——若含 JDBC 连接串，将 `characterEncoding=utf8mb4` 改为 `characterEncoding=UTF-8`。
2. **前端依赖/技术栈清单**——删除不存在的 `vite-plugin-leaflet`。
3. **前后端交互章节**——若描述了响应判断逻辑，将 `code === 200` 改为 `success === true`（与后端 ApiResponseDto 的 `{success, message, data}` 契约一致）。
4. **源码清单/文件结构**——新增 `test-documents/` 下 4 个测试数据文件；确认不含 `target/`、`node_modules/`、`package-lock.json` 等被忽略产物。
5. **目录名**——如曾登记中文目录 `测试文档`，改为 `test-documents`。
6. **核心技术方案**——本次修改**未改变**系统「前后向隐私 + 空间密文检索」的核心算法与功能架构，该部分章节无需改动。

---

*本说明基于 2026-08-01 本地部署调试过程及 git 提交 `cc9ffe2 "fix: 修复本地部署问题并增加测试文档"` 的实际变更整理。*
