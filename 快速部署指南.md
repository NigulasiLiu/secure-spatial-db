# 前后向隐私的空间密文检索系统 V1.0 — 快速部署指南

> 目标环境：华为云开发者空间 / 4核 8G / Linux (Ubuntu 22.04 或 CentOS 7+)
> 部署方式：Docker Compose 一键部署

---

## 一、环境要求

| 项目 | 最低要求 | 推荐配置 | 当前环境 |
|------|---------|---------|---------|
| CPU | 2 核 | 4 核 | ✅ 4 核 |
| 内存 | 2 GB（需调优） | 4 GB+ | ✅ 8 GB |
| 磁盘 | 20 GB | 40 GB+ | — |
| 操作系统 | Ubuntu 20.04 / CentOS 7 | Ubuntu 22.04 LTS | — |
| Docker | 20.10+ | 24.0+ | 待检查 |
| Docker Compose | v2.0+ | v2.20+ | 待检查 |

**4核 8G 完全满足推荐配置，无需任何内存调优。**

---

## 二、前置条件检查与安装

### 2.1 检查 Docker 是否已安装

```bash
docker --version
docker compose version
```

如果两条命令都返回版本号，跳到 [第三节](#三上传项目代码)。

### 2.2 安装 Docker（Ubuntu）

```bash
# 更新包索引
sudo apt-get update

# 安装依赖
sudo apt-get install -y ca-certificates curl gnupg lsb-release

# 添加 Docker 官方 GPG 密钥
sudo mkdir -p /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg

# 添加 Docker 仓库
echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

# 安装 Docker Engine + Compose 插件
sudo apt-get update
sudo apt-get install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin

# 将当前用户加入 docker 组（免 sudo）
sudo usermod -aG docker $USER
newgrp docker
```

### 2.3 安装 Docker（CentOS）

```bash
sudo yum install -y yum-utils
sudo yum-config-manager --add-repo https://download.docker.com/linux/centos/docker-ce.repo
sudo yum install -y docker-ce docker-ce-cli containerd.io docker-compose-plugin
sudo systemctl enable docker
sudo systemctl start docker
sudo usermod -aG docker $USER
newgrp docker
```

### 2.4 国内加速（可选但推荐）

华为云环境拉取 Docker Hub 镜像可能较慢，配置加速器：

```bash
sudo mkdir -p /etc/docker
sudo tee /etc/docker/daemon.json <<'EOF'
{
  "registry-mirrors": [
    "https://docker.mirrors.ustc.edu.cn",
    "https://hub-mirror.c.163.com"
  ]
}
EOF
sudo systemctl daemon-reload
sudo systemctl restart docker
```

### 2.5 验证安装

```bash
docker --version
docker compose version
docker run hello-world
```

三条命令均有正常输出即安装成功。

---

## 三、上传项目代码

### 方式 A：通过 Git 拉取（推荐）

如果代码已推送到 Codeup/GitHub：

```bash
git clone <你的仓库地址> secure-spatial-db
cd secure-spatial-db
```

### 方式 B：通过 SCP 上传

在本地机器执行：

```bash
# 将整个项目目录打包
tar czf secure-spatial-db.tar.gz secure-spatial-db/

# 上传到华为云服务器（替换为实际 IP）
scp secure-spatial-db.tar.gz user@<服务器IP>:~/

# 在服务器上解压
ssh user@<服务器IP>
tar xzf secure-spatial-db.tar.gz
cd secure-spatial-db
```

### 方式 C：通过 rz/sz 上传

```bash
# 在服务器上执行
rz    # 弹出文件选择窗口，上传打包好的 tar.gz
tar xzf secure-spatial-db.tar.gz
cd secure-spatial-db
```

---

## 四、一键部署（核心步骤）

### 4.1 确认目录结构

```bash
ls -la
```

应看到以下关键文件：

```
docker-compose.yml
backend/
frontend/
test-documents/
README.md
```

### 4.2 一键启动

```bash
docker compose up -d --build
```

**这一条命令会自动完成：**
1. 拉取 MySQL 8.0 镜像
2. 拉取 MinIO 镜像
3. 构建 Spring Boot 后端镜像（Maven 编译 + 打包）
4. 构建 Vue 前端镜像（npm install + Vite 构建 + Nginx 托管）
5. 初始化数据库（自动执行 schema.sql 创建 5 张表）
6. 初始化 MinIO 存储桶
7. 按依赖顺序启动全部服务

**首次构建预计耗时 10-20 分钟**（主要在 Maven 依赖下载和 npm install）。

### 4.3 查看启动状态

```bash
docker compose ps
```

全部服务状态应为 `running`：

```
NAME              STATUS              PORTS
ssdb-mysql        Up (healthy)        0.0.0.0:3306->3306/tcp
ssdb-minio        Up (healthy)        0.0.0.0:9000->9000/tcp, 0.0.0.0:9001->9001/tcp
ssdb-minio-init   Up (command exited 0)
ssdb-backend      Up                  0.0.0.0:8080->8080/tcp
ssdb-frontend     Up                  0.0.0.0:80->80/tcp
```

### 4.4 查看日志（如有异常）

```bash
# 查看全部服务日志
docker compose logs -f

# 只看后端日志（最常出问题的服务）
docker compose logs -f backend

# 只看前端日志
docker compose logs -f frontend
```

---

## 五、验证部署

### 5.1 前端页面

浏览器访问：

```
http://<服务器IP>
```

应看到登录页面，标题为「前后向隐私的空间密文检索系统」。

### 5.2 后端 API

```bash
curl http://localhost:8080/api/auth/login -X POST -H "Content-Type: application/json" -d '{"username":"test","password":"test"}'
```

应返回 JSON 响应（用户不存在之类的错误也说明后端正常运行）。

### 5.3 注册并登录

```bash
# 注册
curl http://localhost:8080/api/auth/register -X POST -H "Content-Type: application/json" -d '{"username":"admin","password":"admin123"}'

# 登录
curl http://localhost:8080/api/auth/login -X POST -H "Content-Type: application/json" -d '{"username":"admin","password":"admin123"}'
```

登录成功应返回 `{"success":true,"message":"登录成功","data":{"token":"...","expiresIn":86400}}`。

### 5.4 MinIO 控制台

浏览器访问：

```
http://<服务器IP>:9001
```

用户名 `ssdb_minio`，密码 `ssdb_minio_2026`。

### 5.5 MySQL 连接

```bash
docker exec -it ssdb-mysql mysql -ussdb -pssdb_pass_2026 -e "SHOW TABLES;" ssdb
```

应列出 5 张表：

```
edb_k
edb_p
doc_meta
ss
user
```

---

## 六、服务端口清单

| 服务 | 端口 | 用途 | 访问方式 |
|------|------|------|---------|
| 前端 (Nginx) | 80 | Web 界面 | http://<IP> |
| 后端 (Spring Boot) | 8080 | REST API | http://<IP>:8080 |
| MySQL | 3306 | 数据库 | <IP>:3306 |
| MinIO API | 9000 | 对象存储 | http://<IP>:9000 |
| MinIO Console | 9001 | 管理界面 | http://<IP>:9001 |

**华为云安全组需放行端口 80、8080、9001**（9000 和 3306 仅内部使用，不建议对外暴露）。

---

## 七、日常运维命令

```bash
# 停止全部服务
docker compose down

# 停止并删除数据卷（⚠️ 清空数据库和文件存储）
docker compose down -v

# 重启单个服务
docker compose restart backend
docker compose restart frontend

# 重新构建并启动（代码更新后）
docker compose up -d --build

# 查看资源占用
docker stats

# 进入后端容器调试
docker exec -it ssdb-backend sh

# 进入 MySQL 命令行
docker exec -it ssdb-mysql mysql -ussdb -pssdb_pass_2026 ssdb
```

---

## 八、本地开发模式（不使用 Docker）

如需在服务器上直接运行前后端进行调试：

### 8.1 安装依赖

```bash
# Java 17
sudo apt install -y openjdk-17-jdk maven

# Node.js 20
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs

# MySQL 8（或用 Docker 单独跑 MySQL）
docker run -d --name ssdb-mysql -p 3306:3306 \
  -e MYSQL_ROOT_PASSWORD=ssdb_root_2026 \
  -e MYSQL_DATABASE=ssdb \
  -e MYSQL_USER=ssdb \
  -e MYSQL_PASSWORD=ssdb_pass_2026 \
  -v $(pwd)/backend/src/main/resources/db/schema.sql:/docker-entrypoint-initdb.d/01-schema.sql:ro \
  mysql:8.0

# MinIO（或用 Docker 单独跑 MinIO）
docker run -d --name ssdb-minio -p 9000:9000 -p 9001:9001 \
  -e MINIO_ROOT_USER=ssdb_minio \
  -e MINIO_ROOT_PASSWORD=ssdb_minio_2026 \
  minio/minio server /data --console-address ":9001"
```

### 8.2 启动后端

```bash
cd backend
export DB_HOST=localhost
export DB_PASSWORD=ssdb_pass_2026
export MINIO_ENDPOINT=http://localhost:9000
export MINIO_ACCESS_KEY=ssdb_minio
export MINIO_SECRET_KEY=ssdb_minio_2026
export MINIO_BUCKET=encrypted-files
export JWT_SECRET=ssdb_jwt_secret_key_2026_08_01_forward_backward_privacy
mvn spring-boot:run
```

### 8.3 启动前端

```bash
cd frontend
npm install --registry=https://registry.npmmirror.com
npm run dev
```

前端开发服务器默认在 `http://localhost:5173`，API 请求自动代理到 `http://localhost:8080`。

---

## 九、故障排除

### 9.1 后端启动失败：数据库连接超时

```bash
# 检查 MySQL 是否就绪
docker compose logs mysql
docker exec -it ssdb-mysql mysqladmin ping -h localhost -u root -pssdb_root_2026
```

MySQL 健康检查通过后后端会自动重连，通常等待 30 秒即可。

### 9.2 前端构建失败：npm install 超时

前端 Dockerfile 已配置淘宝镜像源。如仍超时，进入容器手动安装：

```bash
docker compose build --no-cache frontend
```

或修改 `frontend/Dockerfile` 中的 registry 地址为华为云内部镜像源。

### 9.3 端口被占用

```bash
# 查看端口占用
sudo lsof -i :80
sudo lsof -i :8080

# 停止占用进程或修改 docker-compose.yml 中的端口映射
```

### 9.4 内存不足（4核8G 不会遇到此问题）

如部署到 2核2G 机器，使用低内存编排：

```bash
docker compose -f docker-compose.yml -f deployment/docker-compose.lowmem.yml up -d --build
```

并创建 2GB swap：

```bash
sudo fallocate -l 2G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile
```

### 9.5 华为云安全组未放行端口

在华为云控制台 → 安全组规则 → 添加入站规则：

| 端口 | 协议 | 来源 | 说明 |
|------|------|------|------|
| 80 | TCP | 0.0.0.0/0 | 前端访问 |
| 8080 | TCP | 0.0.0.0/0 | API 调试（生产可关闭） |
| 9001 | TCP | 0.0.0.0/0 | MinIO 管理台（生产可关闭） |

---

## 十、系统架构速览

```
┌─────────────────────────────────────────────────┐
│                   浏览器（客户端）                 │
│  ┌──────────┐  ┌──────────┐  ┌──────────────┐   │
│  │ Vue 页面  │  │ 加密引擎  │  │ 密钥管理(Map) │   │
│  │ (9 views)│  │(rskq-cli)│  │ (rootKey等)  │   │
│  └────┬─────┘  └────┬─────┘  └──────────────┘   │
│       │              │                           │
└───────┼──────────────┼───────────────────────────┘
        │ HTTPS        │ 加密 Token + 密文
        ▼              ▼
┌─────────────────────────────────────────────────┐
│              Nginx (端口 80)                      │
│  静态资源 + /api 反代到 8080                      │
└──────────────────────┬──────────────────────────┘
                       ▼
┌─────────────────────────────────────────────────┐
│         Spring Boot 后端 (端口 8080)              │
│  ┌─────────┐ ┌──────────┐ ┌────────────────┐    │
│  │Controller│→│ Service  │→│ RSKQServer     │    │
│  │ (3个)    │ │ (3个)    │ │ (密文链遍历)   │    │
│  └─────────┘ └────┬─────┘ └───────┬────────┘    │
│                     │              │             │
│              ┌──────┴──────┐ ┌────┴─────────┐   │
│              │MySQLStorage │ │MinioStorage  │   │
│              │  Adapter    │ │  (文件存储)  │   │
│              └──────┬──────┘ └────┬─────────┘   │
└─────────────────────┼─────────────┼─────────────┘
                      ▼             ▼
              ┌──────────┐  ┌──────────┐
              │ MySQL 8  │  │  MinIO   │
              │ (5张表)  │  │ (密文桶) │
              │ 端口3306 │  │ 端口9000 │
              └──────────┘  └──────────┘
```

**数据流**：用户输入坐标+关键词 → 浏览器端 RSKQClient 生成加密 Token → 后端密文链式检索 MySQL → 返回密文 → 浏览器端解密展示。服务器全程不接触明文。

---

## 十一、文件清单

| 类别 | 文件数 | 代码行数 | 说明 |
|------|--------|---------|------|
| 后端 Java | 39 | 1,513 | Spring Boot 全栈 |
| 后端配置 | 6 | 256 | pom.xml + yml + sql + Dockerfile |
| 前端 JS | 10 | 963 | 含加密引擎 5 文件 |
| 前端 Vue | 9 | 1,209 | 含 8 页面 + App.vue |
| 前端配置 | 6 | 128 | package.json + vite + nginx + Dockerfile |
| 部署 | 4 | 450 | docker-compose + 部署脚本 |
| 根目录 | 3 | 252 | docker-compose.yml + README + CI |
| **合计** | **77** | **4,771** | — |

---

## 十二、快速命令速查

```bash
# ========== 一键部署 ==========
cd secure-spatial-db
docker compose up -d --build

# ========== 查看状态 ==========
docker compose ps

# ========== 查看日志 ==========
docker compose logs -f backend

# ========== 停止 ==========
docker compose down

# ========== 重新构建（代码更新后） ==========
docker compose up -d --build

# ========== 清空所有数据重新开始 ==========
docker compose down -v
docker compose up -d --build

# ========== 查看资源占用 ==========
docker stats
```
