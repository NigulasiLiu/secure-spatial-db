# secure-spatial-db 排障文档（给原作者"龙虾"）

> 版本：2026-08-02
> 主题：DSSE-RSKQ 空间检索 miss 根因（前缀深度错位）+ 独立发现的哈希域分离缺陷
> 排障方式：本地小型模拟（复用真实算法核心 hilbert.js + Node 复刻 prfSplit/blake2b/jsbn）+ 昨晚真实 rootKey 复刻交叉验证

---

## 一、结论速览

| # | 问题 | 性质 | 是否导致 miss | 建议 |
|---|------|------|--------------|------|
| 1 | 搜索前缀比上传层级链前缀**深 1 bit**，indexKey 对不上 | **根因（已 100% 铁证）** | ✅ 是 | 改前缀生成范式（见三） |
| 2 | 哈希函数 H1-H5 **无域分离**（全部 `blake2b(16,null,null)`），且 H1/H3/H4 输入完全相同 | **独立安全缺陷**（非 miss 根因） | ❌ 否 | 建议按论文核对，补域分离（见四） |
| 3 | 搜索**消费性删除**（命中即删） | 既有行为 | ⚠️ 会加重重复搜索 miss | 确认是否符合设计意图 |

---

## 二、本轮新增：小型模拟（复用真实算法核心）

### 2.1 模拟脚本
- 文件：`frontend/sim_search.mjs`（本地排障工具，非项目源码）
- 方法：直接 `import` 项目的 `src/crypto/hilbert.js`（真实 geoToHilbert/preCode/spatialRangeToPrefixes/prefixToString），
  用 `node:crypto` HMAC-SHA256 复刻 `webcrypto.prfSplit`，用 `blake2b-wasm` 复刻 `blake2b-hash.js`，
  用 `jsbn` 复刻 `bigint-util.js`。流程与 `rskq-client.js` 的 `updateAdd` / `generateSearchToken` 完全一致。

### 2.2 模拟结果
```
[UPLOAD] 上传点 (116.31761, 39.97910)  → preCode 生成 24 个层级前缀,最长24位,存储 cnt=0
[SEARCH] 范围 (116.28,39.95)-(116.36,40.03) → spatialRangeToPrefixes 生成 2 个范围覆盖前缀(24位)
[DIRECT]    搜索前缀直接命中上传前缀: 1/2
[OFFSET]    搜索前缀深于上传祖先、可回溯救回的错位前缀: 1/2
            search=101101001100111000111001 (24位) -> 祖先=10110100110011100011 (20位) 差4bit
[BACKTRACK] 搜索前缀+回溯祖先前缀 命中: 2/2 ✅
[E2E]       直接搜索取数1；回溯后取数2（1条经回溯祖先替换命中）
```

### 2.3 昨晚真实铁证复现（search 18位 vs upload 17位）
```
[铁证] 上传链路含17位祖先: ✅
[铁证] 直接查18位前缀(当前): ❌ miss  ← 复现根因
[铁证] 回溯到17位祖先: 搜索前缀==上传前缀 ✅ 回溯后查祖先 ✅ 命中
[铁证] 18位 vs 17位 差 1 bit → 前缀深度错位是根因，回溯祖先前缀方案可修复
```

### 2.4 模拟结论
**算法本身（密钥派生 / tableKey / counter / 哈希）没有问题**。miss 的根因 100% 是
**上传 `preCode`（单点层级前缀）与 搜索 `spatialRangeToPrefixes`（范围覆盖前缀）产生的前缀深度不一致**：
- 上传最长 17 位（`...10110100110011100`）
- 搜索唯一非零前缀 18 位（`...101101001100111000`）
- 差 1 bit → indexKey 不同 → kx 派生不同 → tableKey 不同 → 后端 miss

---

## 三、根因与修复方向（需作者确认设计意图）

### 3.1 现象
搜索范围矩形内的网格单元经 `spatialRangeToPrefixes` → `binaryPrefixCover` 生成的前缀，
倾向**更深**以便表达矩形边界；而上传点用单点 `preCode` 生成到该点精度的层级前缀（通常更浅）。
当搜索前缀比某个已上传的祖先前缀深 ≤ 额外几 bit 时，该搜索前缀在 edb_p 中**从未被上传过**，导致 miss。

### 3.2 两个候选修复方向（作者主导）

**方向 A：回溯祖先前缀（改动小，仅前端）**
搜索时对"直接未命中"的范围前缀，从长到短截断 1 bit，找到**已上传过的最长祖先前缀**，改用该祖先的 indexKey 生成 token。
- 优点：改动集中在 `generateSearchToken` 内部，不动核心加密/索引范式。
- 代价：回溯命中的祖先前缀会**覆盖到比原搜索范围更大的区域**（范围扩大化），可能把附近文档也返回，需要在解密后按原范围再过滤，或接受粒度变粗。
- 已用模拟验证可行（2.2/2.3）。

**方向 B：上传也改用法范围前缀覆盖（改动中等）**
让 `updateAdd` 使用与搜索**同源**的范围前缀逻辑，或让 `preCode` 补全到与搜索一致的前缀深度上限，
保证上传前缀集合**包含**所有搜索可能命中的更深前缀的祖先。
- 优点：从根源上消除深度错位，语义最干净（符合"范围检索"直觉）。
- 代价：涉及上传侧索引范式改动，需作者对照论文确认，且会增加单次上传的索引条目量。

**建议**：倾向方向 A（回溯），改动小、可快速验证；方向 B 作为论文级彻底修复由作者定夺。

---

## 四、独立发现：哈希函数域分离缺陷（建议反映给作者核对论文）

### 4.1 事实（代码确认，后端 `LAMBDA=128` bit / 16 字节输出，前端 LAMBDA=16 字节）

`frontend/src/crypto/blake2b-hash.js` —— **H1 到 H5 五个函数完全相同**：
```js
export function h1(input) { const hash = blake2b(16, null, null); ... }  // 无 key、无 personalization
export function h2(input) { const hash = blake2b(16, null, null); ... }
export function h3(input) { const hash = blake2b(16, null, null); ... }
export function h4(input) { const hash = blake2b(16, null, null); ... }
export function h5(input) { const hash = blake2b(16, null, null); ... }
```
即 **H1 ≡ H2 ≡ H3 ≡ H4 ≡ H5**（同一 Blake2b-128，无任何域分离参数）。

### 4.2 更严重：`rskq-client.js` 中 H1/H3/H4 的输入也完全相同

updateAdd（同样 updateDelete）：
```js
const h1Input = concatBytes(kx,  cntBytes);   // H1 → tableKey + chainLink
const h2Input = concatBytes(kxPrime, cntBytes); // H2 → eId
const h3Input = concatBytes(kx,  cntBytes);   // H3 → eOp
const h4Input = concatBytes(kx,  cntBytes);   // H4 → state/newState
```
`h1Input == h3Input == h4Input`，且 `h1==h3==h4`（同一函数）⇒
**`H1(kx∥cnt) ≡ H3(kx∥cnt) ≡ H4(kx∥cnt)`**。

只有 H2 因输入不同（`kxPrime∥cnt`）而不同。

### 4.3 安全影响分析（严重，需作者确认）

由于 H1≡H3≡H4 且输入相同，服务器同时持有 chainLink、eId、eOp、state：
```
chainLink = H1(kx∥cnt) ⊕ fId
eOp       = H3(kx∥cnt) ⊕ op      // op ∈ {0,1}
eId       = H2(kxPrime∥cnt) ⊕ fId // H2 不同，受保护
state_new = H4(kx∥cnt) ⊕ state_prev ⊕ fId
```
因为 `H3(kx∥cnt) == H1(kx∥cnt)`：
```
fId = chainLink ⊕ eOp ⊕ op      ← 服务器只需枚举 op∈{0,1} 即可恢复明文文件ID!
```
**即：服务器可在搜索 token 的 kx 已知时，直接由 chainLink + eOp 恢复出文件 ID 与搜索位置前缀的关联**，
破坏了"服务器不可见明文文件ID/查询意图"的机密性目标。这很可能不符合论文中 DSSE 的域分离设计。

### 4.4 建议
- 按论文核对 H1-H5 是否应使用**不同的域分离域**（例如 `H_i(x) = Blake2b-128(domain_i ∥ x)` 或不同 personalization/key）。
- 若论文确实要求域分离，当前实现需重构为带域参数的哈希，且**前端后端要同步改**（后端 `HashFunctions.hashBytes` 也需对应加域参数，保证 token 派生一致）。
- ⚠️ 注意：此缺陷**不直接导致本次 miss**（miss 根因是前缀深度），但属于独立的密码学一致性隐患，建议一并修复。

---

## 五、既有关注点：搜索消费性删除

`RSKQServer.executeSearch`（backend/src/main/java/com/ssdb/engine/RSKQServer.java:52）命中后执行
`removeEncryptedEntry`，即**每个文档只能被搜索命中一次**，命中即从 edb_p 删除。
- 后果：重复搜索同位置会 miss（条目已被消费）。
- 请作者确认这是 DSSE 后向隐私（Type-I⁻）的设计意图，还是应改为"搜索只读、不删"。

---

## 六、环境与验证信息

- 项目托管：github.com/NigulasiLiu/secure-spatial-db
- 当前分支 commit：`32ac060 fix precover`（工作区干净）
- 关键文件：
  - 前端前缀/空间：`frontend/src/crypto/hilbert.js`
  - 前端加密：`frontend/src/crypto/rskq-client.js`、`webcrypto.js`、`blake2b-hash.js`、`bigint-util.js`
  - 后端查询：`backend/.../engine/RSKQServer.java`、`HashFunctions.java`
- 模拟复现：`frontend/sim_search.mjs`（`node sim_search.mjs`）
- 账号：liubinqi/123456、admin/admin123；MinIO ssdb_minio/ssdb_minio_2026；MySQL ssdb/ssdb123
